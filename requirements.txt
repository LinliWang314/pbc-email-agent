# Core dependencies — all pure-Python or ship prebuilt wheels (no compiler needed).
anthropic>=0.39.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
openpyxl>=3.1.2
PyPDF2>=3.0.0
python-dotenv>=1.0.0
jinja2>=3.1.2
pydantic>=2.5.0

# Optional: image OCR (whiteboard photos, scanned docs). Also needs the `tesseract`
# system binary. The agent degrades gracefully without these — ocr_image returns a
# "needs manual review" result instead of crashing. Install with:
#   pip install -r requirements-ocr.txt
